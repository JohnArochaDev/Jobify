import './Home.css'

const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<div className='homeText' >
			<h2>Home Page</h2>
		</div>
	)
}

export default Home
